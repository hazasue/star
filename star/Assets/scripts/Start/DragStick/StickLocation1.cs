using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StickLocation1 : MonoBehaviour
{
	public static Vector2 StickPos1;
    // Start is called before the first frame update
    void Start()
    {
		StickPos1 = transform.position;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
