using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StickLocation2 : MonoBehaviour
{
	public static Vector2 StickPos2;
    // Start is called before the first frame update
    void Start()
    {
		StickPos2 = transform.position;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
