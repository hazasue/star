using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StickLocation3 : MonoBehaviour
{

	public static Vector2 StickPos3;

    // Start is called before the first frame update
    void Start()
    {
		StickPos3 = transform.position;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
